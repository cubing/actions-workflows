{
  "extends": "./node_modules/@cubing/dev-config/ts/es2022-types/tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  // TypeScript excludes dot dirs by default, so we have to explicitly include ours.
  "include": ["**/*", ".github/**/*"]
}
